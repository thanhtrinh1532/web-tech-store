import React from 'react';
import './LoginPage.css'; // Tạo file CSS nếu cần

const LoginPage = () => {
  return (
    <div>
      <h1>LOGIN</h1>
      <p>This is the admin page.</p>
    </div>
  );
};

export default LoginPage; // Export mặc định